{
  "name": "ops-agent",
  "version": "0.1.0",
  "description": "Drive Ops Agent RuntimeTasks with canonical incident-analysis method skills.",
  "skills": "./skills/",
  "mcpServers": "./.mcp.json",
  "defaultEnabled": true,
  "keywords": ["incident", "operations", "rca", "mcp"],
  "userConfig": {
    "runtime_endpoint": {
      "description": "Ops Agent Runtime MCP Streamable HTTP endpoint",
      "sensitive": false
    }
  }
}
