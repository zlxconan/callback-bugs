schema_version = "1.0"
name = "hypothesis-generation"
skill_type = "BUILTIN_METHOD"
version = "1.0.0"
entrypoint = "SKILL.md"
core_api = "1.0"
description = "Generate falsifiable, evidence-aware incident hypotheses from bounded context."
input_contracts = ["HypothesisGenerationRequest"]
output_contracts = ["HypothesisSet"]
mcp_tools = ["incident_get_state", "code_search"]
